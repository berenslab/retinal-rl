# @package _global_
defaults:
  - brain: pure-cnn
  - override /dataset: cifar10
  - _self_

command:
  plot_inputs: false

experiment: cifar10-classification

training:
  num_folds: 5
  recon_weight: 0.5
  num_epochs: 20
  checkpoint_step: 5
  learning_rate: 0.0003
